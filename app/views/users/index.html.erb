<h1>All Users</h1>

<% @users.each do |user| %>

    <%= link_to user_path(user) do %>
        <div>
            <h2><%= user.name %></h2>
            <h3><%= user.position %></h3>
            <img src=<%= user.image_url %>>
        </div>
    <% end %>

<% end %>
